# Core dependencies for Colab TPU environment
# Install with: pip install -r requirements-colab.txt

# TPU stack: rely on the preinstalled Colab versions to avoid mismatches.
torch>=2.0.0
# torch-xla is provided by the runtime; leave unspecified so pip does not clobber it.

# Core dependencies
numpy>=1.24
einops>=0.7
tqdm>=4.66
omegaconf>=2.3

# W&B for experiment tracking (optional but recommended)
wandb[media]>=0.17

# RL environments
gymnasium>=0.29
crafter>=1.8.0

# FAISS is optional and CPU-only; skip on TPU
# faiss-cpu>=1.7.4

# Visualization (for Colab notebooks)
matplotlib>=3.7
imageio>=2.31
pillow>=10.0
